
/**
 * Enumeration of access modes.
 */
export enum AccessMode {
    ReadOnly    = 'RO', // Read-only access.
    WriteOnly   = 'WO', // Write-only access.
    ReadWrite   = 'RW'  // Read and write access.
}